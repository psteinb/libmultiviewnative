# - Config file for the FooBar package
# It defines the following variables
#  MULTIVIEWNATIVE_INCLUDE_DIRS - include directories for Lowpassfilter
#  MULTIVIEWNATIVE_LIBRARIES    - libraries to link against
#  MULTIVIEWNATIVE_EXECUTABLE   - the bar executable

# Compute paths
get_filename_component(MULTIVIEWNATIVE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(MULTIVIEWNATIVE_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${MULTIVIEWNATIVE_CMAKE_DIR}/lowpassfilter-targets.cmake")

# These are IMPORTED targets created by lowpassfilter-targets.cmake
set(MULTIVIEWNATIVE_LIBRARIES ${PROJECT_NAME})
